{
  "name": "profebustos-platform-frontend",
  "version": "1.0.0",
  "description": "Frontend platform for Prof. Bustos website",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && node scripts/inject-version.js",
    "watch": "tsc --watch",
    "test": "jest",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.4",
    "@types/jest": "^30.0.0",
    "husky": "^8.0.0",
    "jest": "^30.0.4",
    "jest-environment-jsdom": "^30.0.4",
    "rimraf": "^5.0.10",
    "ts-jest": "^29.4.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "dotenv": "^17.2.0"
  }
}
